
class PitTile:
    def CheckIfHitPitTile(self, playerX, playerY, pitX, pitY):
        distanceAmount = 20
        #if playerX >= pitX[1].center_x -distanceAmount and playerX <= pitX[1].center_x + distanceAmount and playerY >= pitY[1].center_y -distanceAmount and playerY  <= pitY[1].center_y + distanceAmount:
        #    print("Loads Next Level")
        #    return True
        #return False
        for i in range(len(self.pit_list)):
            #print(self.pit_list[i])
            pass



        
    